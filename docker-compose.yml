version: '3.8'
services:
  redis:
    image: redis:6.2-alpine
    restart: always
    ports:
      - '6379:6379'
    command: redis-server --save 20 1 --loglevel debug --notify-keyspace-events EA
    volumes:
      - cache:/data
volumes:
  cache:
    driver: local